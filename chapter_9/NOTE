1 想一下,为什么需要类和模块
2 JavaScript的链式特点,对属性和对象的声音和定义,要尽量使用增量的方式
  Range.prototype.include = function(x) { return this.from <=x && x <= this.to; }
  替代
  Range.prototype = {
    include: function(x) { return this.from <=x && x <= this.to; } 
  }
3 